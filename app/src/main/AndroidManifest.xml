<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manna_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Friend_select_list_dialog_activity"
            android:theme="@style/AppThemeDialog" />
        <activity android:name=".AddScheduleActivity" />
        <activity android:name=".SearchPlaceActivity" />
        <activity
            android:name=".ShowDetailSchedule_Activity"
            android:theme="@style/AppThemeDialog" />
        <activity android:name=".SettingPersonalRoutine" />
        <activity android:name=".MakingPromise" />
        <activity android:name=".EditProfile" />
        <activity android:name=".MainAgreementActivity" />
        <activity android:name=".Login_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="ozkoxla3i2" />
    </application>
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>